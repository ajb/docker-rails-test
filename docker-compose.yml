version: '2'
services:
  web:
    build: .
    # command: ./app/script/start
    ports:
      - "3000:3000"
    env_file: .env
    # depends_on:
    #   - db
    volumes:
      - .:/app
  # db:
  #   image: postgres:latest
  #   ports:
  #     - "5432:5432"
